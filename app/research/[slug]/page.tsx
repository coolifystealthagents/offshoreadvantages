import {notFound} from 'next/navigation'; import type {Metadata} from 'next'; import {Header,Footer} from '../../components'; import {researchPosts,ResearchPost} from '../../fleet-data'; import {site} from '../../data';
const readerDate=new Intl.DateTimeFormat('en-US',{year:'numeric',month:'long',day:'numeric',timeZone:'UTC'});const formatReaderDate=(value:string)=>readerDate.format(new Date(`${value}T00:00:00Z`));
export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const post=researchPosts.find(p=>p.slug===slug);if(!post)return {};return {title:post.title,alternates:{canonical:`/research/${post.slug}`},openGraph:{type:'article',url:`https://${site.domain}/research/${post.slug}`,title:post.title,description:post.excerpt}}}
export default async function ResearchArticle({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post:ResearchPost|undefined=researchPosts.find(p=>p.slug===slug);if(!post)notFound();const jsonLd={'@context':'https://schema.org','@type':'Article',headline:post.title,description:post.excerpt,datePublished:post.published,mainEntityOfPage:`https://${site.domain}/research/${post.slug}`};return <><Header/><main><article className="section"><div className="container article-shell"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><p className="eyebrow">{site.brand} research · {post.cluster}</p><h1>{post.title}</h1><p className="lead">{post.excerpt}</p><p><time dateTime={post.published}>{formatReaderDate(post.published)}</time> · {post.sources.length} sources · Research methodology</p><section className="card"><h2>Key stats</h2><ul>{post.stats.map(stat=><li key={stat}>{stat}</li>)}</ul></section>{post.sections.map(s=><section className="card" key={s.heading}><h2>{s.heading}</h2><p>{s.body}</p></section>)}<section className="card"><h2>Numbered Sources</h2><ol>{post.sources.map(s=><li key={s.url}><a href={s.url} rel="noreferrer">{s.name}</a></li>)}</ol></section><section className="card"><h2>Related Research</h2><ul>{researchPosts.filter(p=>p.slug!==post.slug).slice(0,3).map(p=><li key={p.slug}><a href={`/research/${p.slug}`}>{p.title}</a></li>)}</ul></section></div></article></main><Footer/></>}
