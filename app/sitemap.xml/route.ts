const aug19Paths=Object.entries(aug19Meta).map(([slug,value])=>`/${(value as {family:string}).family}/${slug}`);const aug20Paths=Object.entries(aug20Meta).map(([slug,value])=>`/${(value as {family:string}).family}/${slug}`);import aug19Meta from '../aug19-meta.json';import aug20Meta from '../aug20-meta.json';import * as data from '../data'; import {fleetServices,researchPosts,postsPerPage} from '../fleet-data';
export function GET(){const d=data as any;const base=`https://${d.site.domain.toLowerCase()}`;const blogs=d.allBlogPosts||[];const pages=Math.max(1,Math.ceil(blogs.length/postsPerPage));const paths=[...aug19Paths,...aug20Paths,'','/services','/blog','/blog/top-50-offshore-outsourcing-companies','/research','/contact-us','/privacy','/terms','/cancellation-policy',...fleetServices.map(s=>`/services/${s.slug}`),...blogs.map((b:any)=>`/blog/${b.slug}`),...Array.from({length:Math.max(0,pages-1)},(_,i)=>`/blog/page/${i+2}`),...researchPosts.map(r=>`/research/${r.slug}`)];const body=Array.from(new Set(paths)).map(path=>`<url><loc>${base}${path}</loc></url>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${body}</urlset>`,{headers:{'content-type':'application/xml'}})}
